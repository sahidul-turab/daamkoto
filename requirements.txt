# Scraping
playwright>=1.44.0

# Cleaning & matching
rapidfuzz>=3.9.0

# Database
psycopg2-binary>=2.9.9
sqlalchemy>=2.0.0

# Backend
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0

# Frontend
streamlit>=1.36.0
plotly>=5.0.0

# AI chatbot (Groq free tier — OpenAI-compatible)
groq>=1.0.0

# Product image background removal (OFFLINE processor only — scripts/remove_backgrounds.py).
# Heavy (onnxruntime + a ~170 MB model on first run); NOT imported by the web backend,
# which only serves the finished transparent PNGs as static files.
rembg>=2.0.77
onnxruntime>=1.27.0
pillow>=10.0.0

# Utilities
python-dotenv>=1.0.0

# Tests. Dev-only — nothing in backend/, scrapers/ or cleaning/ imports pytest,
# so deployment images can skip this line.
#   python -m pytest tests/ -q
pytest>=8.0.0
