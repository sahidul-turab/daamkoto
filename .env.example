# Copy this file to .env and fill in your credentials.
# .env is gitignored — never commit real passwords.

DB_HOST=localhost
DB_PORT=5432
DB_NAME=pc_comparison
DB_USER=postgres
DB_PASSWORD=your_password_here

# Groq API key (free tier — https://console.groq.com)
# GROQ_API_KEY=gsk_...
