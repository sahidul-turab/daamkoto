# Copy this file to .env and fill in your credentials.
# .env is gitignored — never commit real passwords.

DB_HOST=localhost
DB_PORT=5432
DB_NAME=pc_comparison
DB_USER=postgres
DB_PASSWORD=your_password_here

# ── Free AI keys (at least one required for the chat agent) ──────────────────

# Groq API key — fast search queries (free tier — https://console.groq.com)
# GROQ_API_KEY=gsk_...

# Google Gemini API key — complex reasoning: builds, compat, multi-step (free)
# Get one at https://aistudio.google.com/app/apikey
# GEMINI_API_KEY=AIza...
